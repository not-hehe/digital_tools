"""Ядро чекера, разложенное по слоям.

    config    настройки и московское время
    urls      нормализация адреса - нужна и списку, и проверке
    sources   откуда берётся список доменов
    probe     как проверяется домен
    alerting  что делать с результатом: память, тексты, отправка

Зависимости идут в одну сторону: `config` и `urls` не зависят ни от чего,
`sources` и `probe` стоят на них, `alerting` стоит на `probe`. Точка входа
(`main.py`) знает только про пакеты, но не про файлы внутри них.
"""
